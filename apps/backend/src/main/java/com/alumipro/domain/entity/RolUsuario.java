/* archivo de enum rolusuario */
package com.alumipro.domain.entity;

/**
 * Roles del sistema.
 * Los valores deben coincidir exactamente con el ENUM definido en MySQL:
 *   ENUM('ADMIN','VENDEDOR')
 */
public enum RolUsuario {
    ADMIN,
    VENDEDOR
}
