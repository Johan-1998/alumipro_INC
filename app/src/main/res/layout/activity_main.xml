<?xml version="1.0" encoding="utf-8"?>
<!-- ALUMIPRO — Main Activity · Bottom nav dark industrial -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ap_surface"
    android:orientation="vertical">

    <!-- Toolbar: dark antracita con acento flame en el logo -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbarMain"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/ap_base"
        android:minHeight="?attr/actionBarSize"
        android:paddingStart="4dp"
        android:paddingEnd="4dp"
        android:title="ALUMIPRO"
        android:titleTextColor="@color/ap_text_on_dark"
        app:titleTextAppearance="@style/TextAppearance.Alumipro.Toolbar.Title"
        app:menu="@menu/main_top_menu" />

    <!-- Línea flame de 2dp debajo del toolbar -->
    <View
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="@drawable/bg_toolbar_flame_line" />

    <!-- Contenedor de fragmentos -->
    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Bottom Navigation: dark, selección flame -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/ap_base"
        app:itemIconTint="@color/selector_bottom_nav"
        app:itemTextColor="@color/selector_bottom_nav"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu" />

</LinearLayout>
